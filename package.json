{
  "name": "arterns",
  "version": "1.0.1",
  "description": "for internship",

  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "karan singh kanyal",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
